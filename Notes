Math.abs() won't work at Integer.MIN_VALUE

To Print 1D Array:
System.out.print(Arrays.toString(a));

To Print Multidimensional Array:
System.out.print(Arrays.deepToString(a));

Traverse a Set :
for(int i : set){

}

Insert Multiple elements in List at a time :
List<Integer> temp = Arrays.asList(a[i],a[j],a[k],(int)fourth);

Iterate in HashMap :
for(Map.Entry<Integer, Integer> i : mp.entrySet()){
            if(i.getValue() > n/3)
}

Map Increment count [freq of elements] :
            mp.put(sum, mp.getOrDefault(sum, 0)+1);


Print List<List<String>> :

List<List<String>> ans = solveNQueens(4);
        for(List<String> board : ans){
            for(String row : board){
                System.out.println(row);
            }
            System.out.println();
        }
